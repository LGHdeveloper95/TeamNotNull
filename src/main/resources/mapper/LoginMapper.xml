<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guguin.login.mapper.LoginMapper">

<select id="getUser" resultType="com.guguin.login.vo.LoginVo">
    SELECT USERID,USERPW
    FROM PERSONAL_MEMBERS
    WHERE USERID=#{userid} AND USERPW=#{userpw}
</select>

<select id="getCom" resultType="com.guguin.login.vo.LoginVo">
  SELECT COMID,COMPW
   FROM BUSINESS_MEMBERS
   WHERE COMID=#{comid} AND COMPW=#{compw}
</select>


</mapper>